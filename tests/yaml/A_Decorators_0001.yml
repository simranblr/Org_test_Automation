pipelines:
- name: A_Decorators_0001
  decorators:
  - name: sample_decorator
    execution:
      beforeOnStart:
      - echo "before start"
      afterOnStart:
      - echo "after start"
      beforeOnComplete:
      - echo "before complete"
      beforeOnSuccess:
      - echo "before success"
  steps:
  - name: A_Decorators_0001
    type: Bash
    decorators:
      - name: sample_decorator
    execution:
      onStart: 
      - echo "on start"
      onComplete:
      - echo "on complete"
